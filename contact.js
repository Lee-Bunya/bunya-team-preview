document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('contact-form');form.querySelector('button[type=submit]').disabled=false;form.addEventListener('submit',e=>{e.preventDefault();if(form.reportValidity())document.getElementById('contact-status').textContent='The required fields are complete. This is a preview: nothing has been sent or saved.';});form.addEventListener('input',()=>{document.getElementById('contact-status').textContent='';});});
