document.addEventListener('DOMContentLoaded',()=>{const filters=document.querySelector('.in-filters');if(!filters)return;const cards=[...document.querySelectorAll('.in-card')];filters.hidden=false;filters.addEventListener('click',event=>{const button=event.target.closest('button[data-topic]');if(!button)return;filters.querySelectorAll('button').forEach(item=>item.setAttribute('aria-pressed',String(item===button)));let count=0;cards.forEach(card=>{card.hidden=button.dataset.topic!=='all'&&card.dataset.category!==button.dataset.topic;if(!card.hidden)count++;});document.getElementById('in-count').textContent=count+' article'+(count===1?'':'s');});});