document.addEventListener('submit',e=>{const form=e.target;if(!form.matches('[data-bunya-form],#contact-form'))return;e.preventDefault();e.stopImmediatePropagation();if(!form.reportValidity())return;let status=form.querySelector('[role="status"]')||document.getElementById('form-status')||document.getElementById('contact-status');if(!status){status=document.createElement('p');status.setAttribute('role','status');form.append(status)}status.textContent='Preview only: nothing has been sent or saved.';},true);document.querySelectorAll('form button[type="submit"]').forEach(b=>b.disabled=false);