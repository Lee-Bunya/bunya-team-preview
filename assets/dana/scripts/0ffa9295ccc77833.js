(()=>{const cards=[...document.querySelectorAll('.ins-card')],buttons=[...document.querySelectorAll('[data-topic]')],search=document.getElementById('ins-search'),more=document.querySelector('.ins-more'),status=document.getElementById('ins-status');let topic='all',limit=6;function render(){const q=search.value.trim().toLowerCase();const matches=cards.filter(c=>(topic==='all'||c.dataset.category===topic)&&(!q||c.textContent.toLowerCase().includes(q)));cards.forEach(c=>c.hidden=true);matches.slice(0,limit).forEach(c=>c.hidden=false);more.hidden=matches.length<=limit;status.textContent=matches.length?'Showing '+Math.min(limit,matches.length)+' of '+matches.length+' articles':'No matching articles. Try another keyword or topic.';buttons.forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.topic===topic)))}buttons.forEach(b=>b.addEventListener('click',()=>{topic=b.dataset.topic;limit=6;render()}));search.addEventListener('input',()=>{limit=6;render()});more.addEventListener('click',()=>{limit+=6;render()});render()})();