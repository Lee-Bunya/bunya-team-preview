(()=>{const main=document.getElementById('faq-search'),ask=document.getElementById('fg-ask'),chips=[...document.querySelectorAll('.fg-chips button')];if(!main||!ask)return;const run=(q,go)=>{main.value=q;main.dispatchEvent(new Event('input',{bubbles:true}));if(go)main.closest('.faq-search').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'start'})};chips.forEach(b=>b.addEventListener('click',()=>{chips.forEach(c=>c.setAttribute('aria-pressed',String(c===b)));ask.value=b.textContent;run(b.dataset.q,true)}));ask.addEventListener('input',()=>{chips.forEach(c=>c.setAttribute('aria-pressed','false'));run(ask.value,false)});ask.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();run(ask.value,true)}});document.querySelector('.fg-send').addEventListener('click',()=>run(ask.value,true));main.addEventListener('input',e=>{if(e.isTrusted){ask.value='';chips.forEach(c=>c.setAttribute('aria-pressed','false'))}})})();